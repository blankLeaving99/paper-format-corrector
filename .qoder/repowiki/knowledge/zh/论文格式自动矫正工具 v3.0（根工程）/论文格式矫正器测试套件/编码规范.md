- 测试文件顶部统一插入 `src` 目录到 sys.path，使 `from paper_format_corrector.*` 可直接工作。
- 需要磁盘文件的测试一律通过 `tmp_path` 夹具创建临时路径，不在项目根目录落盘。
- 被测模块在测试函数体内进行局部 import，而非模块级导入，以便按需加载并减少启动开销。
- fixture 命名遵循 `<领域>_<用途>_path` 模式（如 `sample_paper_path`、`requirement_natural_path`），返回字符串路径供用例消费。
- 集成测试使用 class 分组（如 `TestCLIArgumentParsing`、`TestDocumentAnalyzerIntegration`）并在方法 docstring 中说明断言目标。
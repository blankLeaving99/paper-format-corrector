- 插件以 Python 类形式实现，通过类级属性 name、description、priority 声明元信息，而非构造函数参数。
- 插件必须实现 process(self, doc, context) 进行文档处理并通过修改 context 字典传递中间结果，同时提供 get_report() 返回结构化报告。
- YAML 模板使用分层键名（如 format_rules.font、format_rules.headings.heading1、auto_detect.*_pattern）组织规则，并以注释标注单位与含义。
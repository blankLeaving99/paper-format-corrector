- python-docx 采用 PartFactory 注册表模式：在 `__init__.py` 中将内容类型常量映射到具体 Part 类，并在末尾 `del` 清理命名空间以避免污染导入路径。
- BDD 测试按领域拆分 feature 文件（如 `doc-add-paragraph.feature`、`tbl-cell-props.feature`），每个 feature 对应 `features/steps/` 下的同名 Python step 实现，形成一一对应关系。
- SKILL 包遵循 YAML front-matter + Markdown 的结构化描述约定，通过 `---` 分隔元数据块，CLI 命令示例与 Agent 使用准则分节呈现，便于外部 Agent 解析。
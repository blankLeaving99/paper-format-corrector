- 可选功能一律通过 pyproject.toml 的 optional-dependencies 声明，并在代码中以 try/except ImportError 懒加载，避免未安装时崩溃。
- GUI 入口（desktop_gui.py / gui.py）与 CLI（cli.py）均暴露 main() 函数，由 run.py 统一 import 调用，禁止在子模块中直接写 if __name__ == "__main__" 分支。
- YAML 模板与全局配置集中在 presets/ 与 config/ 目录，core 引擎只通过相对路径读取，不得硬编码模板内容。
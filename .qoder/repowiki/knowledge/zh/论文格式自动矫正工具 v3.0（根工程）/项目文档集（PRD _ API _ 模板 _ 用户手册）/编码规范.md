- 所有对外接口以表格形式描述：方法、路径、Content-Type、参数表、响应体与错误码一一对应，便于自动生成 OpenAPI/Swagger。
- YAML 模板字段采用「键名 — 类型/默认值 — 说明」的行内注释风格，并在正文中提供字号 pt ↔ 中文字号对照表等速查表。
- 每个可执行入口均给出最小可复现的命令行或 curl 示例，再展开详细参数说明，遵循「先跑通、再细读」的阅读顺序。